// IGame* game = create_tictactoe_game();  // Or whatever game
// INeuralNet* net = create_neural_net("resnet", game);

// // Use the neural network
// net->init(net, game);
// // ... (train, predict, etc.)

// // Clean up
// net->destroy(net);
// free(net);